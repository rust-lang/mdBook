# Introduction

This page is watched by `mdbook watch` / `mdbook serve`. Edit the file
and the next build will pick up the change.

The example below is the kind of snippet that should live in
`examples/` and be pulled in via the `links` preprocessor.

```rust
fn main() {
    println!("served by mdbook-go");
}
```
