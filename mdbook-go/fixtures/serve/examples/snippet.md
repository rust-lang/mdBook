# Snippet

This file lives under `examples/`, an extra-watch-dir. Changes here
must trigger a rebuild even though the source scanner does not see
them by default.

```rust
pub fn add(a: i32, b: i32) -> i32 { a + b }
```
