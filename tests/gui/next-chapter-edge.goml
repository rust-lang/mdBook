// The next-chapter control should sit flush with the right viewport edge
// so a click at the screen edge hits the button (see rust-lang/mdBook#3202).

go-to: |DOC_PATH| + "all-summary/index.html"
set-window-size: (1400, 800)
reload:

assert-css: (".nav-chapters.next", {"right": "0px"})
